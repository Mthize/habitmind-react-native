// AI-powered insights will be available only for paid users after paywall integration.
// Do not expose API keys in client code.

const placeholderMessage = "Gemini insights are not enabled yet.";

export async function generateHabitInsight() {
  return placeholderMessage;
}

export async function generateMoodInsight() {
  return placeholderMessage;
}

export async function generateWeeklySummary() {
  return placeholderMessage;
}
